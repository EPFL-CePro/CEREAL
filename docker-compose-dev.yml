services:
  cereal-app-db:
    env_file:
      - path: .env
        required: true
    image: mysql
    container_name: cereal-app-db
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: secret
      MYSQL_DATABASE: "${MYSQL_DATABASE}"
      MYSQL_USER: "${MYSQL_USER}"
      MYSQL_PASSWORD: "${MYSQL_PASSWORD}"
    healthcheck:
      test: ["CMD", "mysqladmin" ,"ping", "-h", "localhost"]
      timeout: 5s
      retries: 10
    volumes:
      - ./db:/docker-entrypoint-initdb.d
    networks:
      - cereal-app-network
  cereal-app:
    container_name: cereal-app
    image: node:20
    ports:
      - '3000:3000'
    volumes:
      - .:/app
      - ${EXAM_FILES_UPLOAD_FOLDER}:${EXAM_FILES_UPLOAD_FOLDER}:rw
    stdin_open: true
    tty: true
    command: /bin/sh -c "cd /app && npm install && npm run dev"
    depends_on:
      cereal-app-db:
        condition: service_healthy
    environment:
      - NODE_ENV=development
      - EXAM_FILES_UPLOAD_FOLDER=${EXAM_FILES_UPLOAD_FOLDER}
    env_file:
      - .env
    networks:
      - cereal-app-network
  cereal-app-pma:
    image: phpmyadmin
    container_name: cereal-app-pma
    restart: unless-stopped
    environment:
      - PMA_HOST=${MYSQL_HOST}
    depends_on:
      cereal-app-db:
        condition: service_healthy
    ports:
      - 8088:80
    networks:
      - cereal-app-network

networks:
  cereal-app-network:
    name: cereal-app-network